
var playBtn = document.querySelector("button[type=submit]");
var choiceTopic = document.querySelector("#choice")


playBtn.addEventListener("click", function load() {
    if (choiceTopic.value !== "select") {
        //This should take the choice topic value and send it to whatever handles the API AJAX Request...


    }
});